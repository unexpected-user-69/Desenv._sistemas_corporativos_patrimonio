Guia Completo 4 Governança + Bootstrap do Microsserviço Users (Aurora) Este guia reúne tudo que já foi feito e todo o embasamento para que você entenda e consiga aplicar práticas de governança e implementação do microsserviço Users na plataforma Aurora. Cobre desde proteção da branch main até a implementação completa do projeto base. Visão Geral e Objetivos Plataforma Aurora Começar a plataforma Aurora pelo microsserviço Users como base sólida para toda a arquitetura Práticas de Governança Aplicar práticas de governança desde o início: PR obrigatório, CI com lint/build/test, proteção da main, histórico linear Repositório Simples Repo simples (um serviço no root). O pipeline já está preparado para uma futura migração para monorepo Governança do Repositório A governança adequada do repositório é fundamental para manter a qualidade e rastreabilidade do código. Implementamos um conjunto robusto de proteções e práticas que garantem que todo código passe por revisão e validação antes de chegar à branch principal. Proteção da Main Configurações rigorosas que impedem commits diretos e garantem que todo código passe por pull requests com validações automáticas Status Checks Validações automáticas de lint, build e test que devem passar antes de qualquer merge, garantindo qualidade contínua Histórico Linear Uso de squash merge para manter um histórico limpo e linear, facilitando rastreamento e rollbacks quando necessárioProteção da Main: Configurações Detalhadas Opção O que garante / Risco mitigado Observação prática Require pull request Todo código entra via PR ³ histórico claro, revisão e CI Mesmo trabalhando solo, PR mantém rastreabilidade Require status checks Evita quebrar a main; gate de qualidade Os 3 jobs existem na CI Require up to date PR precisa revalidar contra a main atual Evita merges "velhos" Enforce admins Ninguém pula as regras por acidente Torna o padrão sólido Disallow force push Protege o histórico e a branch principal Segurança do repositório Configuração via CLI (gh api) Para automatizar e padronizar as configurações de proteção, utilizamos a CLI do GitHub com chamadas diretas à API REST. Isso garante consistência e permite versionamento das configurações. gh api -X PUT -H "Accept: application/vnd.github+json" \ repos/:owner/:repo/branches/main/protection \--input - <<'JSON' {  "required_status_checks": {     "strict": true,     "contexts": ["lint","build","test"]   },  "enforce_admins": true,  "required_pull_request_reviews": {    "required_approving_review_count": 0,    "dismiss_stale_reviews": false,    "require_last_push_approval": false,    "require_code_owner_reviews": false  },"required_conversation_resolution": true, "required_linear_history": true, "allow_force_pushes": false, "allow_deletions": false, "restrictions": null } JSON Cenário solo: PR obrigatório, 0 aprovações exigidas, checks lint/build/test, strict, conversas resolvidas, linear history, sem force push/deleção e enforce admins. Botão de Merge e Histórico Linear Por que Squash Merge? Com required_linear_history=true, merges com merge commit são bloqueados. Deixar só Squash simplifica o fluxo: 1 PR ³ 1 commit. Configuração via CLI gh repo edit \--enable-merge-commit=false \--enable-rebase-merge=false \--enable-squash-merge=true \--delete-branch-on-merge Verificação gh api repos/:owner/:repo | jq '{ allow_merge_commit: .allow_merge_commit, allow_rebase_merge: .allow_rebase_merge, allow_squash_merge: .allow_squash_merge, delete_branch_on_merge: .delete_branch_on_merge }' Benefícios: Histórico limpo, fácil rollback, commits semânticos e rastreabilidade clara de featuresCODEOWNERS, PR Template e Checklist /src/** @org/time-devs @aluno1 @aluno2 /test/** @org/time-devs 1 CODEOWNERS Arquivo .github/CODEOWNERS que define responsáveis automáticos por revisão de código em diferentes partes do projeto 2 PR Template Template em .github/pull_request_template.md que padroniza contexto, evidências e validações em todos os pull requests 3 Checklist de Auditoria Bloco "Auditoria Proteção da main" no README com itens marcados e links/prints de evidência para compliance Estratégia de Scaffold sem Conflitos Problema comum: nest new . em um diretório que já tem README.md / .github/ ³ conflito e aborta. 01 Geração Temporária Criar o projeto Nest em diretório temporário para evitar conflitos com arquivos existentes de governança 02 Cópia Seletiva Usar rsync para copiar apenas os arquivos necessários, preservando .github/, README.md e outras configurações 03 Commit em Branch Fazer commit em branch separada (ex.: feat/nestskeleton) e abrir PR seguindo o fluxo de governançamkdir -p /tmp/nest-skel && cd /tmp/nest-skel nest new aurora-users --package-manager npm --strict --skip-git --skip-install rsync -av /tmp/nest-skel/aurora-users/ \ "/caminho/do/repo/" \--exclude '.git' --exclude '.github' --exclude 'README.md' Instalação e Lockfile Importância do Package-lock.json Rode npm install para gerar o package-lock.json Versione esse arquivo no repositório A CI usa npm ci (mais rápido e reprodutível) O comando npm ci exige lockfile Sem o lockfile versionado, a CI falhará na etapa de instalação das dependências Benefícios Builds reprodutíveis Instalação mais rápida na CI Versões exatas das dependências Segurança contra mudanças inesperadas Ajustes de Lint Necessários no-floating-promises Regra que impede promessas "boiando" sem tratamento adequado async function bootstrap() { /* ... */ } void bootstrap();import request from 'supertest'; await request(app.getHttpServer())  .get('/').expect(200); Família no-unsafe-* Regras que impedem uso de tipos inseguros no código E2E Esses ajustes são essenciais para manter a qualidade do código e evitar falhas na pipeline de CI. As regras de lint garantem que o código siga as melhores práticas de TypeScript e NestJS. CI para Repo Simples O workflow de CI foi projetado para funcionar tanto em repositórios simples quanto em monorepos futuros, garantindo flexibilidade e escalabilidade da solução. Lint Verificação de qualidade do código com ESLint e Prettier Build Compilação do TypeScript e verificação de erros de tipo Test Execução de testes unitários e de integração Em repo simples, use npm run .... Para monorepo, pode-se detectar "workspaces" e usar npm -ws run ... # Workflow .github/workflows/ci.yml com 3 jobs # Mantemos npm ci nos jobs (depende do package-lock.json)Configuração da Porta da Aplicação Porta 3001 Configurada A aplicação está configurada para rodar na porta 3001 por padrão, com flexibilidade para usar variáveis de ambiente. const port = parseInt(process.env.PORT ?? '3001', 10); await app.listen(port); Execução Local PORT=3001 npm run start:dev # Acesse: http://localhost:3001/ Configuração flexível permite diferentes portas em diferentes ambientes (dev, staging, prod) Arquivos Criados e Suas Funções src/main.ts Bootstrap do servidor Nest; define porta e middlewares globais para inicialização da aplicação src/app.module.ts Módulo raiz; registra controllers/services e outros módulos do sistema src/app.controller.ts Controller exemplo inicial da rota GET / com lógica básica de resposta test/app.e2e-spec.ts Teste E2E mínimo de GET / com supertest para validação end-to-end package.json Scripts start, start:dev, build, lint, test, test:e2e usados localmente e na CI package-lock.json Lockfile necessário para npm ci (CI estável/reprodutível)Arquivos de Configuração Arquivo Função nest-cli.json Configuração do Nest CLI (sourceRoot, entry, schematics) tsconfig.json Configuração TypeScript para desenvolvimento tsconfig.build.json Configuração TypeScript específica para build de produção .eslintrc.* Regras de qualidade de código (ESLint) .prettierrc Regras de formatação de código (Prettier) .github/workflows/ci.ym l Pipeline com lint/build/test (checks exigidos pela proteção da main) Conceitos-chave Explicados no-floating-promises Toda Promise deve ser aguardada (await), tratada (.catch) ou marcada como ignorada (void). Evita rejeições silenciosas e race conditions. Família no-unsafe-* Impede chamar/acessar/retornar valores any/unknown sem refino. Melhor tipagem = menos bugs em tempo de execução. Teste E2E Testa o sistema como caixapreta via HTTP, percorrendo o fluxo completo (controller ³ service ³ camadas internas) e verificando resposta.Configurações Jest e TypeScript jest.config.ts Configuração global do Jest para: Transform TypeScript Descoberta de testes Cobertura de código Mapeamentos de módulo test/jest-e2e.json Configuração dedicada aos E2E: Padrões de arquivo específicos RootDir apropriado Setups específicos para testes E2E Scripts do package.json Contrato com a CI e padrão de execução local: start - Produção start:dev - Desenvolvimento build - Compilação lint - Qualidade test - Testes unitários test:e2e - Testes E2E Problemas Reais e Soluções zsh: [] em contexts[] Problema: O shell tenta expandir [] ³ erro Solução: Usar JSON no gh api (robusto) ou escapar/aspas Campos aninhados ignorados Problema: Alguns subcampos não aplicaram com -F Solução: Enviar corpo JSON completo no PUT de proteção 404 em :owner/:repo Problema: Placeholder literal não substituído Solução: Usar owner/repo real ou rodar dentro do repoMais Problemas e Soluções 1 nest new . conflitando Problema: Diretório não vazio com README.md Solução: Gerar em /tmp e copiar com rsync preservando .github/README 2 CI falhando em npm ci Problema: Faltava package-lock.json Solução: Versionar o lockfile no repositório 3 Erros de lint Problema: no-floating-promises e nounsafe-* Solução: void bootstrap() e supertest com default import + await 4 Workflow usando npm -ws Problema: Não há workspaces em repo simples Solução: Detectar "workspaces" e usar npm run quando não houver Como Testar (Local e CI) Testes Locais npm ci npm run lint npm run build npm test npm run test:e2e PORT=3001 npm run start:dev # abrir http://localhost:3001/ Todos os comandos devem executar sem erros antes de abrir um PR CI (no PR) Verificar os 3 jobs: lint ³ build ³ test Todos devem ficar verdes Anexar no PR as evidências Links dos jobs e print da proteção da mainStatus Atual e Próximos Passos 8 Itens Concluídos Proteção da main, CI, skeleton NestJS, lint configurado 5 Próximos Passos PostgreSQL, migrations, CRUD Users, Swagger, health check ' Checklist do que já está pronto Proteção da main aplicada (PR, checks lint/build/test, strict, conversas resolvidas, linear history) Botão de merge ajustado (Squash) PR template e checklist de auditoria no README Skeleton NestJS criado no root sem conflitos Lockfile versionado para npm ci Lint ajustado (main.ts e E2E) CI rodando os 3 jobs para repo simples App pronto para rodar na porta 3001 « Próximos passos (pendentes) Banco PostgreSQL + TypeORM (datasource + Docker Compose + .env.example) Migrations (sem synchronize:true) CRUD de Users (entidade, DTOs, service, controller) com bcrypt Job test da CI com Postgres como service Swagger (/api/docs), ValidationPipe global e rota /health Com este guia, você tem a base completa para continuar a implementar o microserviço Users com governança sólida e pipeline confiável. A partir daqui, é iterar sobre banco, migrations e CRUD, mantendo os mesmos padrões de qualidade.